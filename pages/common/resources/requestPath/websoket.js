/**
 * 请求路径 
 * 
 * payRequestUrl的请求地址
 */
var BaseUrl = require("../common.js");
console.log(BaseUrl);
var websocketpath =BaseUrl.websocketpath; //清远
console.log(websocketpath);
module.exports=websocketpath;